#include <stdio.h>
#include <stdlib.h>

#define MAX_ARRAY_SIZE 10
#define MAX_STRING_SIZE 80


void populate_float_array(float fa[], int array_size);
float gen_rand_float();
int find_index_of_max(float[], int);
void print_float_array(float float_array[], int array_size);
void strappend(char[], char[] ); 
int str_len(char[]);
